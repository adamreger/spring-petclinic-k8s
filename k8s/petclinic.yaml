# ---------------------------
# Namespace
# ---------------------------
apiVersion: v1
kind: Namespace
metadata:
  name: petclinic

---
# ---------------------------
# Customers Service
# ---------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customers
  namespace: petclinic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customers
  template:
    metadata:
      labels:
        app: customers
    spec:
      containers:
        - name: customers
          image: areger55/harness-se-lab-dockerhub-rep:customers-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            # If your services default to H2, leave this out.
            # Uncomment the next line only if you later add MySQL:
            # - name: SPRING_PROFILES_ACTIVE
            #   value: "mysql"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: customers
  namespace: petclinic
spec:
  type: ClusterIP
  selector:
    app: customers
  ports:
    - port: 8080
      targetPort: 8080

---
# ---------------------------
# Vets Service
# ---------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vets
  namespace: petclinic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vets
  template:
    metadata:
      labels:
        app: vets
    spec:
      containers:
        - name: vets
          image: areger55/harness-se-lab-dockerhub-rep:vets-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: vets
  namespace: petclinic
spec:
  type: ClusterIP
  selector:
    app: vets
  ports:
    - port: 8080
      targetPort: 8080

---
# ---------------------------
# Visits Service
# ---------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: visits
  namespace: petclinic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: visits
  template:
    metadata:
      labels:
        app: visits
    spec:
      containers:
        - name: visits
          image: areger55/harness-se-lab-dockerhub-rep:visits-latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: visits
  namespace: petclinic
spec:
  type: ClusterIP
  selector:
    app: visits
  ports:
    - port: 8080
      targetPort: 8080

---
# ---------------------------
# API Gateway
# ---------------------------
apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: petclinic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api-gateway
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
        - name: api-gateway
          image: springcommunity/spring-petclinic-api-gateway:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            # If the gateway needs explicit downstream URLs, set them here.
            # Remove if your build already auto-discovers by service DNS names.
            - name: CUSTOMERS_SERVICE_URL
              value: "http://customers.petclinic.svc.cluster.local:8080"
            - name: VETS_SERVICE_URL
              value: "http://vets.petclinic.svc.cluster.local:8080"
            - name: VISITS_SERVICE_URL
              value: "http://visits.petclinic.svc.cluster.local:8080"
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: petclinic
spec:
  type: NodePort
  selector:
    app: api-gateway
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30808   # change if this port is taken locally
